"use client";

/**
 * Карточки фактов («Коротко, по делу») теперь интегрированы непосредственно
 * в секцию Hero (Hero.tsx) в виде компактного стеклянного бара.
 * Отдельная секция отключена, чтобы исключить дублирование контента.
 */
export function TrustStats() {
  return null;
}
